import 'package:flutter/material.dart';

class PancackesTab extends StatelessWidget {
  const PancackesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("PancackesTab"),
    );
  }
}